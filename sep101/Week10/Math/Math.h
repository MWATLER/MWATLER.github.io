//Math.h - header file for the math features

class Math {
public:
	//4 function prototypes or declarations for the overloaded function square
	int square(int);
	double square(double);
	double square(int numerator, int denominator);
	double square(double numerator, double denominator);
//	double square(float numerator, float denominator);
};